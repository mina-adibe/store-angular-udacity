export interface Product {
  id: number;
  name: string;
  price: number;
  url: string;
  description: string;
  amount: number;
}

export interface User {
  name: string;
  address: string;
  creditCard: string;
}
